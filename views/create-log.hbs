<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Create Log</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
      margin: 0;
      padding: 40px;
      background-color: #f9f9f9;
      color: #333;
    }

    .container {
      max-width: 720px;
      margin: 0 auto;
      background: white;
      padding: 32px;
      border-radius: 12px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.06);
    }

    h1 {
      font-size: 28px;
      margin-bottom: 24px;
    }

    .top-bar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 24px;
    }

    .btn {
      background-color: #007BFF;
      color: white;
      border: none;
      padding: 10px 10px;
      font-size: 14px;
      border-radius: 6px;
      width: 100;
      cursor: pointer;
      transition: background-color 0.2s;
    }

    .btn:hover {
      background-color: #0056b3;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 2px;
    }

    label {
      font-weight: 600;
      font-size: 14px;
      display: flex;
      flex-direction: column;
      gap: 2px;
    }

    input[type="text"],
    input[type="datetime-local"] {
      padding: 10px;
      font-size: 14px;
      border: 1px solid #ccc;
      border-radius: 6px;
    }

    input[type="submit"] {
      align-self: flex-start;
      padding: 10px 10px;
      background-color: #007BFF;
      color: white;
      border: none;
      border-radius: 6px;
      font-size: 14px;
      cursor: pointer;
    }

    input[type="submit"]:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="top-bar">
      <h1>{{title}}</h1>
      <a href="/"><button class="btn">Back</button></a>
    </div>

    <form method="POST" action="/create">
      <label>
        Date
        <input type="datetime-local" name="date" required />
      </label>

      <label>
        Device Name
        <input type="text" name="deviceId" placeholder="device#1" required />
      </label>

      <label>
        Category
        <input type="text" name="category" placeholder="info" required />
      </label>

      <label>
        Description
        <input type="text" name="description" placeholder="Describe what happened..." required />
      </label>

      <input type="submit" value="Send" />
    </form>
  </div>
</body>
</html>
